# Temporal Worker for the application

This folder contains a Spring Boot application that contains the workflow and activities to process.
It also emits SDK Metrics on a Prometheus scrape endpoint /prometheus. 

See the main readme for how to use with Google Cloud Run